import React from 'react';

import * as S from "./styled";

const Input = ({ ...args }) => <S.Input {...args} />;

export const TextArea = ({ ...args }) => <S.TextArea {...args} />;

export default Input;
